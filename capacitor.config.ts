import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ingia.proyectos.slideringia',
  appName: 'SliderINGIA',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
